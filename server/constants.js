const NODE_ENV_VALUE = {
    PRODUCTION: "production",
    DEV: "dev",
};

const CLI_OPTIONS = {
    DEV_CLIENT: "-dev-client",
};

const PRODUCTION_EMAIL_PROVIDER = {
    SPARKPOST: "SPARKPOST",
    MAILGUN: "MAILGUN",
};

module.exports = {
    NODE_ENV_VALUE,
    CLI_OPTIONS,
    PRODUCTION_EMAIL_PROVIDER,
};
